<view class="sys-message">
  <!-- <Top-bar :param="topBarParam" /> -->
  <view class="msg-box">
    <view class="m-warp" tt:for="{{msgList}}" tt:key="{{index}}">
      <view class="m-title">
      <rich-text nodes="{{item.content}}"></rich-text>
      </view>
      
      <view class="m-time">时间：{{item.createdTime}}</view>
    </view>
  </view>
      <!-- <view class="more" catchtap="onMoreClick" tt:if="{{!isShowMore}}">
      加载更多
      </view>
    <view class="more" wx:else>没有更多了</view> -->
  <view class="no-more" tt:if="{{msgList.length <= 0}}">暂无消息</view>
</view>
