<view class="warp">
  <view class="g-msg" tt:if="{{friendList.length > 0}}">圈友通知</view>
  <view class="g-warp" tt:for="{{friendList}}" tt:for-item="v" tt:key="index">
    <view class="g-img">
      <image src="{{v.userPic}}" alt />
    </view>
    <view class="g-title">{{v.nickName}}</view>
    <view class="g-btn" tt:if="{{v.status == 1 && v.applyStatus == 0}}">
      <view class="g-btn1 btn" bindtap="onRefuse" data-v="{{v}}">拒绝</view>
      <view class="g-btn2 btn" bindtap="onAgree" data-v="{{v}}">同意</view>
    </view>
    <view class="g-btn" tt:if="{{v.applyStatus == 1}}">
      <view class="btn3">已添加</view>
    </view>
    <view class="g-btn" tt:if="{{v.applyStatus == -1}}">
      <view class="btn3">已拒绝</view>
    </view>
    <view class="g-btn" tt:if="{{v.status == 0 && v.applyStatus == 0}}">
      <view class="btn3">等待验证</view>
    </view>
  </view>
  <view class="more" bindtap="onMoreClick" tt:if="{{friendList.length > 0}}">
    <template tt:if="{{!isShowMore}}">
      加载更多
      <i class="el-icon-arrow-right"></i>
    </template>
    <template tt:if="{{isShowMore}}">没有更多了</template>
  </view>
  <view class="no-more" tt:if="{{friendList.length <= 0}}">暂无数据</view>
</view>